<article class="bg-gray-100 p-4">
  <div class="container mx-auto bg-white shadow-lg rounded-lg overflow-hidden p-4">
    <h1 class="text-2xl font-bold mb-4">Contenido #<%- data.id %></h1>

    <h2 class="text-xl font-semibold mb-2">Clasificación:</h2>
    <p class="mb-4"><%= data.classification %></p>

    <div>
      <h2 class="text-xl font-semibold mb-2">Encabezado:</h2>
      <p class="mb-4" id="headerText"><%= data.header %></p>
      <button class="copyButton" data-target="#headerText">Copiar Encabezado

        <p class="mb-4 hidden absolute bg-green-200 border border-green-500 text-green-700 px-4 py-3 rounded shadow-lg" id="popupHeader">Copiado!</p>
      </button>

    </div>

    <div>
      <h2 class="text-xl font-semibold mb-2">Contenido:</h2>
      <p class="mb-4" id="contentText"><%= data.content %></p>
      <button class="copyButton" data-target="#contentText">Copiar Contenido

        <p class="mb-4 hidden absolute bg-green-200 border border-green-500 text-green-700 px-4 py-3 rounded shadow-lg" id="popupHeader">Copiado!</p>
      </button>

    </div>

    <div>
      <h2 class="text-xl font-semibold mb-2">Fuente:</h2>
      <p class="mb-4" id="sourceText"><%= data.source %></p>
      <button class="copyButton" data-target="#sourceText">Copiar Fuente

        <p class="mb-4 hidden absolute bg-green-200 border border-green-500 text-green-700 px-4 py-3 rounded shadow-lg" id="popupHeader">Copiado!</p>
      </button>

    </div>

    <h2 class="text-xl font-semibold mb-2">Fecha:</h2>
    <p class="mb-4"><%= new Date(data.date).toLocaleDateString("es-ES") %></p>
  </div>
  </ar>


  <script>
    $(document).ready(function() {
      // crear una funcion para copiar el texto de header content or source, con click env add ui button too

      $('.copyButton').click(function() {
        var pop = $(this).find('#popupHeader');
        pop.removeClass('hidden');
        setTimeout(() => {
          pop.addClass('hidden');
        }, 3000);

        var targetId = $(this).data('target');
        var text = $(targetId).text();
        var tempInput = $('<input>');
        $('body').append(tempInput);
        tempInput.val(text).select();
        document.execCommand('copy');
        tempInput.remove();
        alert('Texto copiado: ' + text);
      });


    });
  </script>