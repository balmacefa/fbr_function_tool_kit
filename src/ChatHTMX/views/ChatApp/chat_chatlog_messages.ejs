<%  const chat_messages = locals.chat_data_info.chat_messages.data; %>
<%  for (const message of chat_messages) { %>

<%- await include(GetChatView('chat_single_message'), { message }); %>


<% }; %>


<style>
  .markdoc-content {
  @apply overflow-auto;
  word-break: break-word;
  /* General Styles */
  /* Headings */
  /* Paragraphs */
  /* Lists */
  /* Blockquotes */
  /* Code Blocks */
  }
  .markdoc-content body {
  font-family: sans-serif;
  margin: 0;
  padding: 0;
  }
  .markdoc-content #markdoc_msg_xofqbXkP9EnJmg4fzd2KeqXJ {
  max-width: 800px;
  margin: 0 auto;
  padding: 20px;
  border: 1px solid #ccc;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
  }
  .markdoc-content h1, .markdoc-content h2, .markdoc-content h3, .markdoc-content h4, .markdoc-content h5, .markdoc-content h6 {
  font-weight: bold;
  margin: 20px 0 10px;
  }
  .markdoc-content h1 {
  font-size: 24px;
  }
  .markdoc-content h2 {
  font-size: 20px;
  }
  .markdoc-content h3 {
  font-size: 18px;
  }
  .markdoc-content h4 {
  font-size: 16px;
  }
  .markdoc-content h5 {
  font-size: 14px;
  }
  .markdoc-content h6 {
  font-size: 12px;
  }
  .markdoc-content p {
  margin: 10px 0;
  line-height: 1.5;
  }
  .markdoc-content ul, .markdoc-content ol {
  list-style-type: none;
  padding: 0;
  margin: 0;
  }
  .markdoc-content li {
  margin-bottom: 10px;
  }
  .markdoc-content blockquote {
  margin: 20px 0;
  padding: 10px;
  border: 1px solid #ccc;
  }
  
.markdoc-content article {
  overflow: auto;
}

.markdoc-content pre {
  white-space: pre-wrap; /* Permite que el texto envuelva al contenedor */
  word-wrap: break-word; /* Asegura que las palabras largas se rompan */
  background-color: #f0f0f0; /* Color de fondo */
  padding: 10px; /* Espaciado interno */
  border: 1px solid #ccc; /* Borde */
}


</style>