<div
  class="flex flex-col p-8 items-center justify-center h-full mt-32 mb-8 mx-auto bg-orange-100"
>
  <i class="fas fa-exclamation-triangle fa-4x text-orange-500 mb-4"></i>
  <!-- Exclamation mark icon -->
  <h3 class="text-lg font-semibold text-orange-800">Error del Servidor</h3>
  <p class="text-sm text-orange-600 mt-2 text-center">
    Ha ocurrido un error en el servidor. Por favor, revisa los detalles a
    continuación.
  </p>

  <!-- Collapsible for error details -->
  <div x-data="{ open: false }">
    <button
      x-on:click="open = !open"
      class="mt-4 text-orange-500 hover:text-orange-600"
    >
      Ver Detalles del Error
    </button>

    <div x-show="open" class="mt-2 p-4 bg-orange-200 rounded">
      <p><strong>Detalle del Error:</strong> <%= error___details %></p>
      <pre class="overflow-x-auto mt-2 bg-gray-100 p-3 rounded">
        <code>
          <%= JSON.stringify(message_json, null, 2) %>
        </code>
      </pre>
    </div>
  </div>
</div>
