/**
 *
 * :((*))
 *
 * FBR_INDEX_START
L1 Esto es un texto que no tiene propiedades pero lo que sigue si tiene
L2 Esto es un texto que no tiene propiedades pero lo que sigue si tiene"

{
    "name": "Test App",
    "version": "1.0.0"
}
    FBR_INDEX_END
 */
