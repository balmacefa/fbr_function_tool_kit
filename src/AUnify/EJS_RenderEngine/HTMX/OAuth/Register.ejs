<!--  -->
<%- await include(GetViewPath('Layouts/render_layout_path_top')); %>

<!-- this is no make sure we use our project components  -->
<!-- var header_content = await include(GetViewPath('components/header')); -->
<% var aside = await include(GetViewPath('components/side_menu')); %>

<div class="h-full w-full">
  <!-- First column -->
  <div class="flex flex-col h-full">
    <!-- Header Content -->
    <header class="">
      <!-- header_content  -->
    </header>
    <main class="flex flex-row h-full w-full">
      <aside>
        <%- aside %>
      </aside>
      <article class="container mx-auto p-4">
        <%- main_content_html %>
  </div>
  </article>
  </main>
</div>
</div>


<%- await include(GetViewPath('Layouts/render_layout_path_bottom')); %>