export const Helpers = {
    formatMessage(message: string): string {
        // Lógica para formatear un mensaje
        return message;
    },

    validateInput(input: any): boolean {
        // Lógica para validar una entrada
        return true; // o lógica de validación específica
    },

    // Otros métodos de ayuda generales
};

export default Helpers;
