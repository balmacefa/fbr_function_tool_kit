
<% if (locals.paginated_response) locals.paginated_response.hits.forEach(function(hit_data, index) { %>
    <%- include('one_hit', {
        hit: hit_data,
        headerNumber: index +1
    }) %>
<% }); %>